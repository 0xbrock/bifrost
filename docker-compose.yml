services:
  bifrost:
    image: maximhq/bifrost:latest
    ports:
      - "8080:8080"
    volumes:
      - ./data:/app/data
      - ./config/config.json:/app/data/config.json
    env_file:
      - .env
    environment:
      - APP_HOST=0.0.0.0
    restart: unless-stopped
